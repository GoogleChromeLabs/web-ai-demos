# Word Up

A word guessing game coded using Antigravity 2.0, Modern Web Guidance, and DevTools for Agents utilizing the Prompt API to generate words to guess and hints for players.

## Usage

You need Node >= 24 and PNPM >= 11 to run this app, then run `pnpm dev` to run the dev server. Then, open the URL that runs!